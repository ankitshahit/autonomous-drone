package org.drone.robot.engine.utils;

public class EngineUtils {
	/**
	 * Only to be used for engine project
	 * 1 meaning high state as true for quick conversion.
	 * @param val
	 * @return
	 */
	public static boolean convertToBoolean(int val) {
		return val == 1;
	}
}
